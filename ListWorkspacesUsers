for i in {1..5}; do
    aws workspaces describe-workspaces --directory-id HERE --query 'Workspaces[*].[UserName,State]' --output table >> output.txt 2>&1
    if [ $? -eq 0 ]; then
        break
    fi
    sleep 5  # Aguarde 5 segundos antes de tentar novamente
done